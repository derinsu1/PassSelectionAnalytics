from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parents[3]
APP_DATA_DIR = PROJECT_ROOT / "data" / "app"
SOURCE_MATCHES_DIR = PROJECT_ROOT / "data" / "source" / "skillcorner" / "matches"
